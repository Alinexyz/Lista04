const usuarioCorreto = "fulano"
const senhaCorreta = 40028922

const usuario = "fulano"
const senha = 40028922

if (usuario === usuarioCorreto && senha === senhaCorreta) {
    console.log("Acesso permitido")
} else {
    console.log("Acesso negado")
}